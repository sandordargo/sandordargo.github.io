I"“<p>As usual, letâ€™s examine what its counterpart can be in SQL. In this case it is definitely <code class="language-plaintext highlighter-rouge">SELECT</code>, however <code class="language-plaintext highlighter-rouge">RETURN</code> is a bit simpler. Putting that aside, the most important visual difference between them is that in Cypher you end a query with <code class="language-plaintext highlighter-rouge">RETURN</code> while in SQL you start one with <code class="language-plaintext highlighter-rouge">SELECT</code>. But in both cases, you use them to define what you want to return from your query. In its simplest form you can return everything:</p>
:ET